specVersion: "v1beta"
serviceId: "Geoprofs"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "Geoprofs-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
